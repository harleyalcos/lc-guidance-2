## Agent skills

### Issue tracker

GitHub issue tracker (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.

### Bug branch creation

Create standardized git bugfix branches from raw client bug reports without formal ticket IDs. See `.agents/skills/fix-branch/SKILL.md`.
